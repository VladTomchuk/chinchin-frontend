// Компонент зараз не підключений: ефект розкриття переїхав у бокове меню
// (navbar/DrawerMenu). Файли лишені, щоб кнопку можна було повернути одним
// рядком у layout.tsx.
//
// Пункти беремо зі спільного конфігу навігації, щоб адреси не розходились із
// меню в шапці. Порядок тут зворотний: у FAB список росте вгору, і найближчим
// до великого пальця має бути найчастіший пункт.
import { NAV_ITEMS, type NavItem } from '@/config/navigation';

export type FabItem = NavItem;

export const FAB_ITEMS: FabItem[] = [...NAV_ITEMS].reverse();
